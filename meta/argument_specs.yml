---
argument_specs:
  main:
    options:
      dev_base_manage_python:
        description: Set to `false` on hosts without Homebrew (e.g. plain Linux, CI containers).
        type: bool
        default: true
        version_added: 1.0.0
      downloads:
        description: Configuration value for downloads
        type: str
        default: ~/Downloads/
        version_added: 1.0.0
...
